# ─── Roots Phenotyping - Rhizotrons ───────────────────────────────
# Image processing, semantic root segmentation, and root trait extraction
# from rhizotron images for quantitative Root System Architecture (RSA) phenotyping.
# Authors: Cristian Tirado-Murcia*, Jorge Aragon, Jose Polania
# *c.tirado@cgiar.org
# Date: 2026-01-06

pandas>=1.5.0

# ============================================
# Distortion correction using ArUco markers
# ============================================
tqdm>=4.65.0

# ── Computer Vision ───────────────────────────────────────────
# NOTA: opencv-python fijado en 4.10.0.84 por requisito de inference==1.0.5
#       (rango soportado: >=4.8.1.78,<=4.10.0.84). No actualizar sin
#       revisar antes la compatibilidad con inference.
opencv-python==4.10.0.84
numpy==2.0.2
Pillow==11.3.0
matplotlib==3.9.4
plantcv==4.10.2

# ── Roboflow Inference (root_segmentation.py) ─────────────────
inference==1.0.5

# ── Utilities ─────────────────────────────────────────────────
# NOTA: python-dotenv fijado en rango ~1.0.0 por requisito de inference==1.0.5
#       (inference exige python-dotenv~=1.0.0)
python-dotenv~=1.0.0
pyzbar==0.1.9

# ── RSA Pipeline - Root Trait Extraction ──────────────────────
scikit-image>=0.21.0
scipy>=1.11.0
networkx>=3.2.0
